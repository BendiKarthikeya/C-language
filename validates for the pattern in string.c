//write a programme that scans a string and validates for the pattern as shown 
#include<stdio.h>
#include<string.h>
int main(){

char a[1000],z;
gets(a);
int count=0;
int len=strlen(a);
printf("%d",len/2);
char c
if(a[len/2]=='122'){

for(int i=0;i<len/2;i++){
	if(a[i]==a[len-1]){
		 count=count+1;
	}
	
}
if(count==(len/2)){
   printf("it is in the correct pattern");
}
}


else{
	printf("Invalid");
}

}